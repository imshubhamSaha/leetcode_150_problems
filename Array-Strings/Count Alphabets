int Count(string s){
    //complete the function here
    int n = s.length();
    int cnt = 0;
    for (int i = 0; i < n; ++i) {
        char ch = s[i];
        if ((ch >= 'a' && ch <= 'z') || (ch >= 'A' && ch <= 'Z')) {
            cnt++;
        }
    }
    return cnt;
}
