/**
 * @param {string} s
 * @return {boolean}
 */
var checkString = function(s) {
    const n = s.length;
    let i = 0;

    while (i < n && s[i] === "a")
        i++;
    
    while ( i < n) {
        if (s[i] === "a")
            return false;
        i++;    
    }

    return true;
};
